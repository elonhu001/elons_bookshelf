在RM的注册函数中通过不同的命令码来调用不同的功能函数，这些功能函数有着共同的入口参数，

C现代编程中的栈同样如此，不同的栈就相当于不同的命令码，根据栈的不同决定调用不同的过滤函数，都是通过函数指针的形式来调用

先定义一个栈的结构体，在new的时候可以决定是否带有校验功能，
然后创建一个校验器，校验器包含两个元素，一个是校验函数，另一个是具体的数据，在new的时候可以指定使用哪种校验器
然后针对不同的校验器创建不同的用于校验的结构体

也就是说，在创建不同的栈的时候就要同时创建不同的校验器